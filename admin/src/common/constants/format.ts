/**
 * Locked display formats — see 05_UI_STANDARDS.md § Date Format / Currency / Numbers.
 * Token casing follows dayjs's format syntax (the locked date library) — the
 * semantic format (day/month/year) matches the doc's `dd/MM/yyyy` spec.
 */
export const DATE_DISPLAY_FORMAT = 'DD/MM/YYYY';
export const DATE_TIME_DISPLAY_FORMAT = 'DD/MM/YYYY HH:mm';
export const CURRENCY_LOCALE = 'en-IN';
export const CURRENCY_CODE = 'INR';
export const NUMBER_LOCALE = 'en-IN';
